// This file is intentionally a placeholder. In production it is never served
// from disk — server/index.js intercepts GET /js/sb-config.js and generates
// this file's real content from environment variables (SUPABASE_URL /
// VITE_SUPABASE_URL and SUPABASE_ANON_KEY / VITE_SUPABASE_ANON_KEY) on every
// request, so a single .env change is enough to point the whole frontend at
// a different Supabase project. Do not hardcode real credentials here.
console.warn('sb-config.js placeholder was served from disk — the server route that generates it dynamically did not intercept this request. Check server/index.js route ordering.');
