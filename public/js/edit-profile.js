document.addEventListener('DOMContentLoaded', async () => {
  if (!Auth.isLoggedIn()) return location.href = '/signin.html';
  const me = Auth.user(), form = document.getElementById('editProfileForm');
  const profile = await API.get(`/marketplace/profile/${me.user_id}`).catch(() => ({}));
  const fields = [['display_name','Display name','text'],['first_name','First name','text'],['middle_name','Middle name','text'],['last_name','Last name','text'],['phone','Phone number','tel'],['country','Country','text'],['state','State / province','text'],['city','City','text'],['address','Residential address','text'],['availability','Availability','text'],['response_time_hours','Typical response time (hours)','number'],['about','About me','textarea'],['cover_letter','Professional summary / cover letter','textarea']];
  form.innerHTML = `<div class="card" style="padding:16px;margin-bottom:16px"><h3>Profile photos</h3><div class="grid grid-2"><div class="form-group"><label class="form-label">Profile photo</label><img id="profilePreview" src="${profile.profile_image || ''}" style="display:${profile.profile_image ? 'block' : 'none'};width:72px;height:72px;object-fit:cover;border-radius:50%;margin-bottom:8px"><input class="form-input" id="profileImageFile" type="file" accept="image/*"></div><div class="form-group"><label class="form-label">Wallpaper / cover image</label><img id="coverPreview" src="${profile.cover_image || ''}" style="display:${profile.cover_image ? 'block' : 'none'};width:100%;height:72px;object-fit:cover;border-radius:8px;margin-bottom:8px"><input class="form-input" id="coverImageFile" type="file" accept="image/*"></div></div></div><div class="grid grid-2">${fields.map(([key,label,type]) => `<div class="form-group"><label class="form-label">${label}</label>${type === 'textarea' ? `<textarea class="form-textarea" name="${key}" rows="4">${profile[key] || ''}</textarea>` : `<input class="form-input" name="${key}" type="${type}" value="${profile[key] || ''}">`}</div>`).join('')}</div><details class="card" style="padding:12px"><summary>Role-specific professional information</summary><p style="color:var(--text-muted)">Skills, education, work history, portfolio, documents, references, languages, and availability details.</p><textarea class="form-textarea" name="profile_sections" rows="10" placeholder='{"skills":["..."],"education":[...],"employment":[...],"portfolio":[]}'>${JSON.stringify(profile.profile_sections || {}, null, 2)}</textarea></details><button class="btn btn-gold btn-lg" style="margin-top:18px">Save profile</button>`;
  [['profileImageFile','profilePreview'], ['coverImageFile','coverPreview']].forEach(([input, preview]) => document.getElementById(input).addEventListener('change', e => { const file = e.target.files[0]; if (file) { const img = document.getElementById(preview); img.src = URL.createObjectURL(file); img.style.display = 'block'; } }));
  form.addEventListener('submit', async e => { e.preventDefault(); const data = Object.fromEntries(new FormData(form)); try { data.profile_sections = data.profile_sections ? JSON.parse(data.profile_sections) : {}; const photo = document.getElementById('profileImageFile').files[0], cover = document.getElementById('coverImageFile').files[0]; if (photo) data.profile_image = await Upload.file(photo); if (cover) data.cover_image = await Upload.file(cover); await API.put('/marketplace/profile', data); Toast.show('Profile saved'); setTimeout(() => location.href = `/profile.html?id=${me.user_id}`, 600); } catch(err) { Toast.show(err instanceof SyntaxError ? 'Profile sections must contain valid JSON.' : err.message); } });
});
